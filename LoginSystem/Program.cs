using LoginSystem.LoginSystem;
using System;

namespace LoginSystem
{
    class Program
    {
        static void Main(string[] args)
        {
            Terminal terminal = new();
            terminal.StartTerminal(false);
        }
    }
}
